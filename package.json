{
  "name": "guestBook",
  "version": "1.0.0",
  "description": "first assignment",
  "main": "app.js",
  "scripts": {
    "start": "if [$NODE_ENV == 'development']; then node app.js; else nodemon app.js; fi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hassan Masood",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^4.3.1",
    "ejs": "^2.6.1",
    "express": "^4.16.4",
    "mustache": "^3.0.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
